/* eslint-disable import/no-default-export */
import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "pl.solvro.eventownik",
  appName: "mobile-eventownik",
  webDir: "dist",
};

export default config;
